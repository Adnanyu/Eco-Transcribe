import { ThemedText } from "@/components/ThemedText";
import { SafeAreaView } from "react-native";

export default function User() {
    return (
      <SafeAreaView>
          <ThemedText>hello world from user page</ThemedText>
      </SafeAreaView>
  );
}